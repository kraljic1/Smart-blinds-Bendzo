import { Product } from '../../types/product';

// Texture Collection
export const textureCollection: Product[] = [
  // Currently no unique Texture products as they were duplicates of Essential products
  // This placeholder allows us to add Texture products in the future
]; 