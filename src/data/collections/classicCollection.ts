import { Product } from '../../types/product';

// Classic Collection
export const classicCollection: Product[] = [
  // Currently no unique Classic products as they were duplicates of Essential products
  // This placeholder allows us to add Classic products in the future
]; 