import { Product } from '../../types/product';

// Screen Collection
export const screenCollection: Product[] = [
  // Currently no unique Screen products as they were duplicates of Essential products
  // This placeholder allows us to add Screen products in the future
]; 